paths:
  outputs:
    step_01_prep: "outputs/step_01_prep/"
  subdirs:
    figures: "figures/"
    tables: "tables/"
    reports: "reports/"

filtering:
  vaf_filtering:
    threshold: 0.5
  coverage_filtering:
    min_coverage: 10
    min_samples: 1

visualization:
  general:
    dpi: 300
    width: 10
    height: 8

logging:
  level: "INFO"

development:
  debug:
    enabled: false
    save_intermediate: false






